# VBA Source

This directory will contain exported Visual Basic for Applications source code from `ProcureFlow.xlsm`.

Expected structure:

- `modules/` — standard `.bas` modules
- `classes/` — class modules if justified later

The workbook remains the executable artifact, while exported VBA files provide readable and diffable source code for Git/GitHub.

No VBA has been implemented yet.
